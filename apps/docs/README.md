# Silo Docs

This app is a Fumadocs-based documentation site for the Silo SDK.

## Development

From the repo root:

```bash
pnpm install
pnpm --filter @silo-storage/docs dev
```

The docs content lives in `content/docs`, and the Fumadocs source config is in `source.config.ts`.
