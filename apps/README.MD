# silo/apps

This directory contains the applications that make up the Silo platform.

- `cf-worker`: The Cloudflare Worker that handles uploads and storage operations
- `nextjs`: The Next.js application that serves the web frontend
- `docs`: The documentation site